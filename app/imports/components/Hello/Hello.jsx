import React, { PropTypes } from 'react';
import { RaisedButton } from 'material-ui';

const Hello = (props) => {
    return (
        <h1>Hello!</h1>
    );
};

Hello.propTypes = {};

export default Hello;
